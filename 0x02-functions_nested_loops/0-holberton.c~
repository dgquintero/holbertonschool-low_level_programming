#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
int n;
char m[] = "Holberton";
int d = sizeof(m);

for (n = 0 ; n < d ; n++)
_putchar(m[n]);
return (0);
}
