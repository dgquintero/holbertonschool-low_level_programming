#include "holberton.h"
/**
 * print_rev - prints a string, in reverse, followed by a new line.
 *
 * @s : is a pointer to a string
 *
 * Return : not return.
 */

void puts2(char *str)
{
	int n;

	for (n = 0; str[n] != '\0'; n+=2)
		_putchar(str[n]);
	_putchar('\n');
}
