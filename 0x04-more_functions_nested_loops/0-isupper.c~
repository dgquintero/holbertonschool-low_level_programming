#include "holberton.h"
/**
 * _isupper - check the code for Holberton School students.
 *
 * @c : parameter to check
 *
 * Return: 1 if is upper case and 0 if is not.
 */
int _isuppe(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
